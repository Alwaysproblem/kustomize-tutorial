apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tensorboard-pvc
spec:
  accessModes:
    - ReadOnlyMany
  volumeMode: Filesystem
  resources:
    requests:
      storage: 500Mi
  storageClassName: s3fs
  selector:
    matchLabels:
      type: s3fs-storage
      app: tensorboard