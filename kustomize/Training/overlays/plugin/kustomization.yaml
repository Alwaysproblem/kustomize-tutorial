apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# namePrefix: yongxi-train-
# commonLabels:
#   app: training

# it a relative directory $path from `kustomize build $path`
bases:
- ../../base

transformers:
  - patch6902.yaml
  - image.yaml
  - prefixsurfix.yaml
  # - commonLabel.yaml
  # - commonAnnotation.yaml
  # - namespaces.yaml